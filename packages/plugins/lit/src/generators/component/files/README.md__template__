# @tompke/<%= name %>

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build @tompke/<%= name %>` to build the library.

## Running unit tests

Run `nx test @tompke/<%= name %>` to execute the unit tests via [Vite](https://vitest.dev/).
